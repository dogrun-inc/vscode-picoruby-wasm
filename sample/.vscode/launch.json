{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch PicoRuby WASM",
            "type": "picoruby-wasm",
            "request": "launch",
            "program": "${file}",
            "stopOnEntry": true,
            "cwd": "${workspaceFolder}",
            "args": []
        }
    ]
}
